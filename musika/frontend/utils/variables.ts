
const hour = 60 * 60;
const variables = {

    caching: {
        categories: hour,
        searchQueries: hour,
        products: hour,
        product_details: hour,
        business: hour,
        catalogs: hour,
        bag: hour,
        profile: hour,
    }
}

export default variables;